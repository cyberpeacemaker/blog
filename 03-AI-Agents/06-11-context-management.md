在探討 **動態提示詞監聽（Dynamic Prompt Listening）** 與 **RAG（檢索增強生成，Retrieval-Augmented Generation）** 之前，我們可以先用一個生動的比喻來理解它們：

如果把 LLM 當作一個**擁有頂級邏輯能力、但缺乏長期記憶的「天才圖書館員」**：

* **RAG** 就是在對話時，工程師一邊聽你的問題，一邊衝進書架裡**抱了一疊相關的最新資料**塞給圖書館員看，讓他能看著資料回答。
* **動態提示詞監聽** 則是工程師在旁邊**默默拿小本本記錄你們過去幾輪的對話**，並在每一次你開口時，把小本本上的重點偷偷塞進你最新的問題裡，讓圖書館員不會前後矛盾。

這兩項技術是現代 AI 應用（如 Line 機器人、企業客服、AI Agent）的核心，也是讓使用者誤以為「AI 在推論時一邊在學習/記住事情」的幕後功臣。

---

## 1. RAG（檢索增強生成）：外掛知識庫

RAG 的出現，是為了解決 LLM 訓練成本高昂、存在知識斷層（Cut-off date）以及胡言亂語（Hallucination）的問題。

### 運作流程

RAG 的運作並非直接修改模型，而是分為以下三個主要步驟：

```
[使用者問題] ──> [向量資料庫檢索] ──> [組合新 Prompt] ──> [LLM 推論] ──> [精準回答]
                      │                     ▲
                      ▼                     │
               (找出最相關的文本) ───────────┘

```

1. **向量化與索引（Indexing）：** 事先將企業內部文件、法規、甚至是你的個人筆記切成小段落（Chunks），透過 Embedding 模型轉換成數學向量，存入**向量資料庫（Vector DB）**。
2. **檢索（Retrieval）：** 當你輸入問題（例如：「公司今年端午節發多少獎金？」），系統會把這個問題也轉換成向量，去資料庫中撈出最相關的前幾名文件段落。
3. **增強與生成（Augmentation & Generation）：** 系統在後台將這些撈出來的文件與你的原始問題打包在一起，變成一個超長的 Prompt 送給模型。

> **實際在後台跑的 Prompt 長這樣：**
> `【系統指令】：請根據以下提供的【參考資料】回答使用者的問題。如果資料中沒有，請老實說不知道，絕對不能編造。`
> `【參考資料】：...公司福利章程第三條：端午節發放半個月底薪作為獎金...`
> `【使用者問題】：公司今年端午節發多少獎金？`

**為什麼讓人誤解？** 因為你明明沒教過 AI 你們公司的福利制度，它卻答得出來。你以為它「學會」了，其實它只是在**開卷考試**。

---

## 2. 動態提示詞監聽（Dynamic Prompt Listening / Context Management）

這通常指的是 AI 應用的後端框架（如 LangChain、Semantic Kernel）中的**上下文管理機制**。LLM 本身是「無狀態的」（Stateless），它不記得上一秒你說了什麼。

為了維持對話的連貫性，開發者必須讓系統具備「聽力」與「動態調整 Prompt」的能力。

### 常見的管理策略

* **全量滑動視窗（Sliding Window）：**
每次你輸入新話語，系統會把這對話串中過去 5 輪、10 輪的歷史紀錄全部黏在你新問題的前面，一起送給模型。隨著對話變長，最舊的對話會被擠出視窗外（遺忘）。
* **動態摘要監聽（Summary Memory）：**
系統在背景派駐另一個微型 AI，默默「監聽」你跟主 AI 的對話。每隔幾輪，這個微型 AI 就會把過去的對話濃縮成一段話（例如：「使用者叫瑞霆，是一位資安工程師，不喜歡密碼輸入，偏好硬體認證」），然後把這段動態更新的摘要一直鎖在 Prompt 的最頂端。
* **意圖與實體抽取（Intent & Entity Extraction）：**
當你說「幫我把昨晚寫的那段 Python 程式碼重構一下」，動態監聽系統會立刻抓出關鍵字：`昨晚`、`Python 程式碼`、`重構`。接著自動去資料庫撈出昨晚的對話紀錄，黏進當前的 Prompt 中。

---

## 兩者的完美結合：現代 AI 助理的完全體

在建構進階的 AI Agent 時，這兩者通常是併行的。

當你對 AI 說：**「根據我們上週討論的架構，幫我寫一份配合 Yubikey 的驗證 API 規格書。」**

1. **動態提示詞監聽** 聽到「上週討論的架構」，立刻轉身去對話歷史資料庫（或記憶模組）中尋找上週的對話摘要與關鍵字。
2. **RAG** 聽到「Yubikey 的驗證 API」，立刻去企業的技術文檔庫或 Yubico 官方的開發指南中，撈出最新、最正確的程式碼範例與規格限制。
3. 系統將「歷史上下文摘要」+「官方技術文檔」+「你當下的指令」融合成一個巨大的 Prompt 送進 LLM。


**這是一場由後端工程、資料庫檢索與前端提示詞共同演出的高超魔術**